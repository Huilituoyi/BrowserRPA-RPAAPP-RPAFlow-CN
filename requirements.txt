# ===== RPAAPP 依赖清单 =====
# 内置浏览器与界面（基于 Chromium 的 QWebEngineView）
PySide6>=6.6.0

# 后台自动化引擎（回放脚本 / 定时任务 / 录制导出目标）
playwright>=1.40.0

# Excel 导出
openpyxl>=3.1.0

# Oracle 数据库（瘦模式，无需安装 Oracle 客户端）
oracledb>=2.0.0

# 定时任务调度
APScheduler>=3.10.0

# OCR 验证码识别服务（ddddocr + Flask）
ddddocr>=1.4.0
flask>=2.0.0
requests>=2.28.0       # 本地测试用

# 安装完依赖后，还需执行一次（下载 Playwright 所需的 Chromium，约几百 MB）：
#   playwright install chromium
